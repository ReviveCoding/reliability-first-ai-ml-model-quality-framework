# Final Evaluation Evidence

This directory contains compact, canonical artifacts supporting the
final model decision. Large run directories, datasets, checkpoints, and
tracking stores remain local and are excluded from Git.

Use `manifest.json` to verify each file's SHA-256 hash and source.
